#ifndef CHANGE_H
#define CHANGE_H

#include <stddef.h>

#include "const.h"

/**
 * @brief Функция записывает простые элементы матрицы в обратном порядке
 *
 * @param a Указатель на матрицу
 * @param m Количество строк
 * @param n Количество столбцов
 * @return int Код ошибки
 */
 
int change(int a[][N_MAX], size_t m, size_t n);

#endif // CHANGE_H
