#ifndef SORT_H
#define SORT_H

#include <stddef.h>

#include "const.h"

/**
 * @brief Функция сортирует строки матрицы по возрастанию произведения элементов
 *
 * @param a Указатель на матрицу
 * @param m Количество строк
 * @param n Количество столбцов
 */
 
void sort(int a[][N_MAX], size_t m, size_t n);

#endif // SORT_H
