#ifndef READ_COUNTS_MULT_H
#define READ_COUNTS_MULT_H

#include <stdio.h>

#include "const_struct.h"

// читаем два целых неотрицательных числа (с проверками)
int read_counts_mult(FILE* file, int *p, int *y);

#endif // READ_COUNTS_MULT_H
